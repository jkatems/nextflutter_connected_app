rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /carnetFeeds/{category} {
      allow get: if request.auth != null && category in ['articles', 'products', 'tasks'];
      allow list, create, update, delete: if false;
    }
  }
}
